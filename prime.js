// function isPrime(n) {
//   if (n <= 1) {
//     return false;
//   }

//   // Check if prime is or not
//   for (var i = 2; i < n; i++) {
//     if (n % i == 0) {
//       return false;
//     }
//   }
//   return true;
// }

// isPrime(18 ) ? console.log("true") : console.log("false");

/**
 * This program finds the largest number in the array [1, 2, 3, 4, 5, 6, 7, 8]
 * and stores it in the variable `eight`.
 * 
 * It then iterates through each element of the array and if an element is greater
 * than the current value of `eight`, it updates `eight` with the new value.
 * 
 * Finally, it logs the value of `eight` to the console.
 */
